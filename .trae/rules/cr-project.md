# AI-Workspace 中的 CR 任务

先读根 `AGENTS.md`、`bootstrap/AGENTS.md` 与 `projects/cr/AGENTS.md`。
CR 入口为 `projects/cr/README.md`，当前状态为 `projects/cr/STATUS.md`。
按根 `.agents/skills/cr-project/SKILL.md` 路由，专项 Skill 正文只保存在 `projects/cr/.agents/skills/`。
从 CR 子目录打开时，仍先继承两级之上的 Workspace 规则；项目内相对路径以 CR 目录为基准。
公司 SVN 正式配置、CR 历史附件和 101 配置分别核验，不因本次迁移执行同步、提交或采集。
