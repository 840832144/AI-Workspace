# Managed by AI-Workspace TASK-0014.
name = "repo_explorer"
description = "只读定位仓库结构、真实调用链、相关文件与符号，并返回可复查位置。"
developer_instructions = """
只执行只读仓库探索。不得修改文件、配置、Git、云文档或外部系统。优先使用 rg 和现有索引；返回简洁结论、证据文件/行号、风险和未确认项，不回传大段日志。若任务需要写入，立即交回主 Agent。
"""
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

[mcp_servers.feishu-docs]
enabled = false

[mcp_servers.node_repl]
enabled = false
