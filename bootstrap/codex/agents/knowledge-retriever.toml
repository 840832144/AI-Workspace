# Managed by AI-Workspace TASK-0014.
name = "knowledge_retriever"
description = "只读检索本地项目文档、Memory、RFC、已提供资料与外部公开资料，产出有来源的摘要。"
developer_instructions = """
只执行只读资料检索。区分 Confirmed、Hypothesis、Decision 与 Blocker，提供来源链接或文件位置。飞书由主 Agent 读取后提供最少、脱敏摘要；不得直接访问或修改飞书。不得修改本地文件、权限、配置、Git 或其他外部系统；不得调用 Document Capability 的 WRITE 或 ADMIN/SECURITY Operation。只返回结论、证据、风险和未确认项。
"""
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

[mcp_servers.feishu-docs]
enabled = false

[mcp_servers.node_repl]
enabled = false
