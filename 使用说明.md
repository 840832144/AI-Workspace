# CR_design 使用说明

本文面向制作人、数值策划和使用 TRAE_CN 继续维护 Cash Royal 数值项目的协作者。目标是让一次需求从“提出问题”完整走到“方案可复算、配置已更新、SVN 可核验”，同时避免误改程序配置快照或把个人电脑路径写进项目。

## 1. 项目解决什么问题

`CR_design` 是 CR 项目的数值策划资源库，主要保存：

- 长期有效的经济、成长、奖励、概率和玩法设计文档。
- 策划源表与程序配置导出快照。
- Python 3 推导、同步、目录构建和 SVN 安全提交工具。
- TRAE_CN 可读取的项目规则与 Skills。
- 飞书资料的筛选归档、来源信息和整理后结论。

本仓库负责“设计、推导、审计和沉淀”。实际上测试服或正式服生效的 Excel，仍应通过对应配置 SVN 的正式制作与发布链路修改。

## 2. 推荐阅读顺序

1. 阅读根目录 `AGENTS.md`，确认数据安全、脚本和交付规则。
2. 从 `数值策划/数值文档/README.md` 进入对应业务模块。
3. 需要查源表时，使用 `数值策划/知识库/策划源表导航.xlsx`。
4. 需要按字段找配置时，使用 `数值策划/知识库/工作簿目录.csv`。
5. 需要核对程序落地时，只读查看 `数值策划/数据源/程序配置导出快照`。
6. 需要 AI 协作时，让 TRAE_CN 读取与任务匹配的 `.agents/skills/*/SKILL.md`。

## 3. 目录职责

| 路径 | 用途 | 是否允许直接改业务值 |
|---|---|---|
| `数值策划/数值文档` | 方案、系统设计、体验报告、复盘 | 可以 |
| `数值策划/工具` | Python 3 工具及数据源配置 | 可以，需可复跑 |
| `数值策划/数据源/策划源表` | 策划原始工作簿快照 | 默认只读；回源目录修改后同步 |
| `数值策划/数据源/程序配置导出快照` | 程序导出配置的审计快照 | 禁止直接修改 |
| `数值策划/知识库` | 自动目录、来源清单、导航 | 自动生成内容不要手改 |
| `.agents/skills` | TRAE_CN 的专项工作流 | 可以，需保持 Skill 规范 |
| `.trae/rules` | TRAE 项目级规则 | 可以，变更前需评审影响 |

## 4. TRAE_CN 如何选择 Skill

| 任务 | 使用 Skill |
|---|---|
| 数值曲线、经济产销、成长、奖励、概率设计 | `cr-numerical-design` |
| 程序配置快照差异、哈希、字段核对 | `cr-audit-config-snapshots` |
| 飞书和本地资料筛选、正文归档 | `cr-ingest-knowledge` |
| 策划工作簿入口、Sheet 和版本治理 | `cr-govern-workbooks` |
| SVN 演练、风险检查、添加、提交与复核 | `cr-svn-submit` |

一个需求可以组合多个 Skill。例如“分析建造难度、修改 dev 配置并提交”应组合：

```text
cr-numerical-design
→ cr-audit-config-snapshots
→ cr-svn-submit
```

## 5. 标准工作流

### 5.1 提需求

需求至少写清：

- 哪个系统、关卡或玩家分层。
- 想改善的体验指标。
- 时间范围或进度范围。
- 哪些配置允许改、哪些必须保持不变。
- 是否要求更新测试服/正式服配置。
- 是否要求 SVN 提交。

推荐提示词：

```text
请使用 CR_design 的项目规则处理：
目标系统：
当前问题：
目标指标：
影响范围：
保持不变：
需要修改的环境：
是否提交 SVN：
最后需要的证据：
```

### 5.2 确认数据源与口径

权威级别从高到低：

1. 正式配置制作与发布链路。
2. 策划原始工作目录。
3. CR_design 内的数据快照。
4. 自动目录、会议记录和聊天信息。

发现同名文件或多个 Sheet 数值不同时，必须记录差异并确认生效口径，不能自行把某一份猜成最终版本。

### 5.3 提出方案

方案至少包含：

- 当前值与目标值。
- 计算公式及单位。
- 影响的行、列、玩家和模块。
- 明确保持不变的字段。
- 边界、风险、回滚方案。
- 验收指标。

### 5.4 修改配置

配置变更必须在正式配置工作副本完成，不能修改 CR_design 的程序配置导出快照。

修改 Excel 时：

- 先保留修改前副本或记录仓库版本。
- 只改目标 Sheet、目标字段和目标记录。
- 保留已有格式、公式、Sheet 和非目标数据。
- 修改后重新读取并做单元格级差异比较。
- 对大型区间至少渲染一次修改前后画面检查。

### 5.5 校验

至少检查：

- 主键重复、空值、零值、负值和边界。
- 修改单元格数量是否等于计划数量。
- 非目标 Sheet 和字段是否发生变化。
- 章节累计值、分段累计值和相对变化。
- 公式是否变化或出现错误。
- SVN 工作副本是否存在冲突、缺失或临时文件。

### 5.6 提交与证据

提交前：

```powershell
svn update
svn status
```

提交后至少提供：

- 提交版本号。
- 提交 URL。
- 提交文件清单。
- 提交说明。
- `svn status` 是否干净。
- `svn log -r <版本号>` 或 `svn info` 的核验结果。

## 6. 完整示例：建筑3中后期提高30%难度

### 6.1 原始需求

> 降低建造模块建筑3的消耗返还，并把建筑3中后期难度提高30%，提出修改方案、更新配置、提交到 dev，并附提交成功证据。

### 6.2 口径定义

| 项目 | 本次定义 |
|---|---|
| 建筑3 | `BuildLevelCfg.level = 3` |
| 中后期 | 60 个配置节点的后 50%，即 `blockNum = 31–60` |
| 难度 | 每个翻地节点的建设币消耗 `needCash` |
| 返还 | Spin、宝箱、荣誉、NPC 等建设币产出保持不变 |
| 返还/消耗比 | 固定返还 ÷ `needCash` |
| 取整 | `ceil(旧值 × 1.30 ÷ 50) × 50` |

没有修改 `BuildCashCheatSheet`，因为该表按 Bet、玩家等级和 VIP 查值，会影响所有建筑，无法只作用于建筑3。

### 6.3 修改方案

只修改 dev 配置：

```text
BuildLevelCfg.xlsx
└─ Sheet1
   └─ level=3, blockNum=31–60
      └─ needCash × 1.30，向上取整到50
```

以下内容保持不变：

- 建筑3前 50% 的 `needCash`。
- 所有节点的 `honorVal`、`expVal`、`npcMax`、`npcOne`。
- `SpinDropBuildingCoins` 的概率与基础掉落数量。
- `BuildCashCheatSheet` 的 Bet/等级/VIP 系数。
- `BuildBox` 和 `BuildHonorLevel` 的奖励。
- 其他建筑的全部配置。

### 6.4 修改前后结果

| 指标 | 修改前 | 修改后 | 变化 |
|---|---:|---:|---:|
| 修改节点数 | 30 | 30 | 仅 block 31–60 |
| 建筑3前半段累计消耗 | 366,900 | 366,900 | 0 |
| 建筑3中后期累计消耗 | 14,500,100 | 18,850,650 | +30.0036% |
| 建筑3全章累计消耗 | 14,867,000 | 19,217,550 | +29.2631% |
| 中后期返还/消耗比 | 100% 基准 | 76.92% | -23.08% |

由于采用 50 为取整单位，每行实际提高范围为 30.0000%–30.0439%。

代表节点：

| blockNum | 旧 needCash | 新 needCash | 变化 |
|---:|---:|---:|---:|
| 31 | 65,600 | 85,300 | +30.0305% |
| 49 | 525,650 | 683,350 | +30.0009% |
| 60 | 1,577,100 | 2,050,250 | +30.0013% |

### 6.5 校验结果

| 检查项 | 结果 |
|---|---|
| 数值差异单元格 | 30 |
| 计划修改单元格 | 30 |
| 公式差异 | 0 |
| 非 `needCash` 字段差异 | 0 |
| 非建筑3差异 | 0 |
| 建筑3前半段差异 | 0 |
| 修改后文件可重新读取 | 通过 |
| 修改前后视觉核对 | 通过 |

### 6.6 dev 提交证据

本次提交完成后，将记录：

| 项目 | 结果 |
|---|---|
| SVN URL | `http://140.143.33.242/svn/cr/x_proj_share/dev/ExcelConfigExport/Excel` |
| 提交文件 | `BuildLevelCfg.xlsx` |
| 提交版本 | `r5421` |
| 提交说明 | `CR 建造：建筑3中后期翻地消耗提高30%` |
| 提交时间 | `2026-07-31 10:52:45 +0800` |
| 提交作者 | `wangkun` |
| 提交后状态 | 工作副本干净，无待提交变更 |
| 文件 SHA-256 | `03858B903CDAED8E01C65E827D268B1A83C6116C882248E7F8A8A73E59DA6FEA` |

### 6.7 风险、监控与回滚

当前建筑3中后期在修改前已经占全章累计消耗的 97.5%，因此本次虽然只改后半段，仍会让全章累计消耗提高 29.2631%。在建设币产出、Bet 选择和在线时长不变时，理论完成时长也会近似按同一比例增加。

上线后建议监控：

- 建筑3的 P50/P90 完成时长。
- block 30、48、49、60 的到达率与流失率。
- 玩家建设币不足次数及平均等待 Spin 数。
- 建筑3阶段的建设币产出/消耗比。
- 调整前后玩家等级、VIP 和 Bet 分层差异。

回滚条件建议：

- 建筑3 P50 完成时长提高超过 35%。
- block 49 后退出率提高超过 5 个百分点。
- 建设币不足导致的连续等待超过目标上限。

需要回滚时，以 r5418 的 `BuildLevelCfg.xlsx` 为基线恢复建筑3 block 31–60 的 `needCash`，重新执行单元格差异校验，并通过一次新的 SVN 提交完成回滚；不直接改写历史版本。

## 7. 常见错误

- 直接修改 `数值策划/数据源/程序配置导出快照`。
- 为了只改一个建筑而调整全局 `BuildCashCheatSheet`。
- 把 `expVal` 当作 Spin 升级；它是翻地后增加的玩家经验。
- 只报“提高30%”，不定义进度区间和取整规则。
- 只看单行，不核对章节累计值和返还/消耗比。
- 提交整个脏工作副本，把无关文件一起带入。
- 只根据 `svn commit` 退出码宣称成功，不核对版本号与日志。
- 在文档或脚本中保存个人电脑绝对路径、账号或密码。

## 8. 完成标准

一次数值配置任务只有同时满足以下条件才算完成：

- 需求口径明确。
- 方案可复算。
- 修改范围可定位到文件、Sheet、字段和记录。
- 非目标内容未变化。
- 风险、回滚和验收指标已写明。
- 配置进入正确发布链路。
- SVN 版本号、日志、文件清单和提交后状态可核验。
