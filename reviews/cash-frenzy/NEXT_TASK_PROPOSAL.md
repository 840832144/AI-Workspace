# TASK-0022 Next Task Proposal — Draft Only

当前不创建新 Task。

若最小动态 proof 达到 F3，建议 ChatGPT Review 后由 User 选择是否建立 **Cash Frenzy Passive Collector Adapter** Task，范围只包含：

- independent instance/package binding；
- `BLSocket` 或等价明文 hook；
- Session/manifest/Raw/inventory；
- 最小 Spin schema mapping；
- planner start/stop/preflight。

若只能到 F2，优先提出 **Protocol Boundary Probe**，不构建 Collector。若仍为 F1/F0，则停止投入并保留 static inventory。

不自动晋升，不分配编号，不执行 Static Config Adapter、Local State Adapter、RTP/EV、长期概率或产品化。

